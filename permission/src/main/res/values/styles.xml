<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Theme used for the permission activity -->
    <style name="Permission.Activity" parent="@style/Theme.AppCompat.NoActionBar">
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowFrame">@null</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowAnimationStyle">@android:style/Animation</item>
        <item name="android:colorBackgroundCacheHint">@null</item>
    </style>
    <!-- Theme used for the permission rationale dialogs -->
    <style name="Permission.RationaleDialog" parent="@style/Theme.AppCompat.Dialog">
    </style>
</resources>